import { createContext } from "react";

const passdata= createContext()
// remember this has container [it can store multiple data]
export default passdata;

// how to inster the valeu 
// how to pass and retrive the vlue from the multiple comp